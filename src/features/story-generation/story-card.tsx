import { StyleSheet, Text, View } from 'react-native';
import type { GeneratedStory } from './story-generation-client';
export function StoryCard({ story }: { story: GeneratedStory }) { return <View style={s.card}><Text style={s.eyebrow}>ORIGINAL STORY</Text><Text accessibilityRole="header" style={s.title}>{story.title}</Text><Text style={s.hook}>{story.hook}</Text><Text style={s.story}>{story.story}</Text>{story.scenes.map((scene, index) => <View key={`${scene.startSeconds}-${index}`} style={s.scene}><Text style={s.time}>{scene.startSeconds}s–{scene.endSeconds}s · {scene.purpose}</Text><Text style={s.line}>{scene.narration}</Text><Text style={s.visual}>{scene.visual} · {scene.emotion}</Text></View>)}<Text style={s.ending}>{story.ending}</Text><Text style={s.note}>{story.originalityNote}</Text></View>; }
const s = StyleSheet.create({ card: { marginTop: 16, padding: 18, borderRadius: 22, borderWidth: 1, borderColor: 'rgba(168,255,26,.35)', backgroundColor: '#0D100B' }, eyebrow: { color: '#A8FF1A', fontSize: 11, fontWeight: '900', letterSpacing: 1.1 }, title: { marginTop: 5, color: '#F7F7F5', fontSize: 22, fontWeight: '800' }, hook: { marginTop: 12, color: '#FFFFFF', fontSize: 17, lineHeight: 24, fontWeight: '700' }, story: { marginTop: 10, color: '#C8CBC5', fontSize: 14, lineHeight: 21 }, scene: { marginTop: 14, paddingTop: 12, borderTopWidth: StyleSheet.hairlineWidth, borderTopColor: 'rgba(255,255,255,.16)' }, time: { color: '#A8FF1A', fontSize: 11, fontWeight: '800' }, line: { marginTop: 4, color: '#F7F7F5', fontSize: 14, lineHeight: 20 }, visual: { marginTop: 3, color: '#929692', fontSize: 12, lineHeight: 17 }, ending: { marginTop: 16, color: '#FFFFFF', fontSize: 15, lineHeight: 21, fontWeight: '700' }, note: { marginTop: 10, color: '#929692', fontSize: 11, lineHeight: 16 } });
